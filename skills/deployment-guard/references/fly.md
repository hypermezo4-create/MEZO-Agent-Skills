# Fly

Use separate web/worker process groups, correct internal ports, health grace periods, minimum machines, concurrency limits, release commands, and secrets. Avoid using ephemeral root filesystems for durable state.
