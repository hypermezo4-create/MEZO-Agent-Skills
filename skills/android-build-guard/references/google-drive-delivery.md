# Google Drive Delivery

Select by exact build manifest, not glob order. Upload to a controlled folder, use resumable transfer, verify remote size/hash or provider checksum, store file ID, and link the verified object to the source commit.
