# Llm App Testing

Prompt tests verify loading, template substitution, required parse markers, schema validation, and flow transitions. They do not pin prose, telemetry call arguments, retry internals, or provider call counts.
